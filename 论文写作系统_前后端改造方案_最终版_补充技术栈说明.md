# 工科/实验型毕业论文写作系统前后端改造方案（最终版）

## 1. 文档目的

本文档用于将原有的“证据链驱动 + 工件/门禁 + 逐节协作写作”方案，进一步升级为**前后端分离的论文工作流系统方案**。目标不是继续依赖单一对话式项目容器来完成论文生产，而是构建一个可长期保存状态、可管理文件、可执行审批与回退、可串行推进实验体系写作的产品化系统。

该方案保留原设计的核心约束：

- 先证据链，后叙事
- 图表与分析先于正文写作
- 写作必须受 Claim–Evidence Matrix 约束
- 每个实验体系独立成章、独立推进、独立确认
- 至少完成三个实验体系章节后，再进入绪论、结论与展望的整合写作

同时，方案参考了上传论文中“按实验体系分章”的典型结构：第 2、3、4 章分别对应不同研究模块，每章内部采用“引言—实验材料与方法—结果与讨论—本章小结”的固定骨架，最终再进入“结论与展望”章节。

---

## 2. 为什么需要从“项目对话模式”升级到“前后端工作流模式”

### 2.1 原模式的核心问题

在实际使用过程中，单纯依赖项目内对话上传和上下文维护，会遇到以下问题：

1. **文献、原始数据、图像、谱图、草稿等异构材料难以稳定管理**
2. **文件版本与章节状态无法长期沉淀**
3. **图表、数据、结论之间的追溯关系难以结构化维护**
4. **用户“无限确认直到满意”的过程无法固化为任务流和审批流**
5. **一旦上下文膨胀，写作质量、可复现性与一致性会明显下降**

### 2.2 为什么前后端模式更适合这个项目

你的原始设计本质上不是一个“聊天生成器”，而是一个**强状态、强工件、强门禁、强追溯**的论文生产系统。原设计已经明确提出：

- Step 3 前置 `Figure/Table Plan`
- Step 4/5/6 形成原始数据、清洗记录、分析报告
- Step 8 生成 `Asset Manifest` 与 `Claim–Evidence Matrix`
- Step 9 大纲必须绑定 asset id
- Step 10 正文只能严格基于 manifest/analysis report 中的证据进行扩写

因此，最合适的形态不是“对话工具 + 文件上传”，而是：

> **一个以实验体系为最小推进单元、以证据矩阵为唯一事实源、以门禁审批为主控逻辑的论文工作流平台。**

---

## 3. 最终架构判断

### 3.1 你提出的方向是否合理

结论：**合理，而且方向正确。**

将系统改为：

- 前端负责上传文献、实验数据、手工图、原始数据与进度监控
- 后端负责流程编排、状态管理、Agent 调度与写作推进
- Claude Code 负责其中一部分结构化执行任务

这比继续依赖项目式对话更符合论文写作场景。

### 3.2 需要修正的地方

但系统不应定义为：

> 前端上传 + 后端 Claude Code 全包

更稳妥的定义应该是：

> **前端工作台 + 业务后端/工作流引擎 + 文件与元数据存储 + Claude Code 作为受控执行器之一**

也就是说，Claude Code 不是系统本体，而是后端执行层中的一个 worker。

---

## 4. 系统目标重定义

### 4.1 产品定义

该系统应定义为：

> **实验体系驱动的毕业论文工作流平台**

而不是：

> “一个能聊天并生成论文的 AI 工具”

### 4.2 三个最重要的系统原则

#### 原则 1：实验体系是最小业务单元


每一个实验体系都应独立拥有：

- 体系卡（System Card）
- 图表计划（Figure/Table Plan）
- 数据请求包（Data Request Pack）
- 资产清单（Asset Manifest）
- 证据矩阵（Claim–Evidence Matrix）
- 章节草稿（Chapter Draft）
- 审批状态（Gate Status）


#### 原则 2：Evidence Matrix 是唯一事实源

写作阶段不得直接根据原始文献或用户口头描述自由生成结论，而必须先由系统将结论整理进 `Claim–Evidence Matrix`，再由写作 agent 严格扩写。

#### 原则 3：门禁驱动，而不是自由对话驱动

系统必须以 G0–G5 门禁推进，而不是靠用户和 agent 自由聊天维持状态。

---

## 5. 顶层系统架构

推荐采用三层式架构：

```text
[前端工作台]
      |
      v
[业务后端 / 工作流引擎 / 任务与审批层]
      |
      +--------------------+
      |                    |
      v                    v
[对象存储 + 结构化数据库]   [Agent 执行层：Claude Code / Vision / 数据分析 Worker]
```

### 5.1 前端负责什么

前端不是普通上传页面，而是一个完整的**论文工作台**。主要负责：

- 项目总览
- 实验体系管理
- 文件上传与归档
- 图表与证据面板
- 章节写作面板
- 门禁审批与状态监控

### 5.2 后端负责什么

后端负责：

- 项目/章节/实验体系状态管理
- 工作流与门禁控制
- 文件元数据管理
- Agent 编排与任务调度
- 数据分析、图像校验、写作请求的执行管理
- 审批、退回、重试、版本追踪

### 5.3 Claude Code 的正确定位

Claude Code 只负责执行受限任务，例如：

- 模板与章节结构提取
- 文献结构归纳
- 实验体系卡生成
- 图表计划生成
- 数据分析辅助
- 小节草稿撰写
- 一致性检查

Claude Code **不负责**：

- 长期状态维护
- 文件生命周期管理
- 权限控制
- 审批流控制
- 系统级版本与回滚逻辑

---

## 6. 前端工作台设计

建议前端包含以下 6 个主模块。

### 6.1 项目总览（Dashboard）

显示：

- 项目名称
- 论文模板
- 当前总阶段
- 已完成实验体系数
- 当前待处理任务
- 当前待审批事项
- 风险提醒（缺数据、缺图、缺证据、缺确认）

### 6.2 实验体系管理（Systems）

每个实验体系以卡片形式展示：

- 体系名称
- 研究目标
- 样品/对象
- 自变量与对照
- 输出指标
- 方法概述
- 当前状态
- 是否允许进入下一门禁

这一模块对应你新的核心写作逻辑：**先定义体系，再围绕体系组织章节**。

### 6.3 文件上传与归档（Assets）

支持上传：

- 学校模板/格式模板
- 参考文献 PDF
- 原始实验数据
- 清洗后表格
- 手工图/谱图/显微图
- 分析报告
- 章节草稿

每个文件必须绑定：

- 项目
- 实验体系
- 图表计划项（可选）
- 文件类型
- 版本号
- 上传时间与上传者

### 6.4 图表与证据面板（Evidence）

集中展示：

- Figure/Table Plan
- Asset Manifest
- Claim–Evidence Matrix
- 证据缺口提醒
- 当前章哪些结论还没有可用证据

这是整个平台的中心。

### 6.5 章节写作面板（Drafting）

每一节都应展示：

- 当前节微大纲
- 已绑定的图表/资产
- 草稿历史版本
- 用户意见与修改记录
- “通过 / 退回修改 / 锁定” 操作

### 6.6 门禁审批面板（Gates）

用于展示：

- G0–G5 各门禁当前状态
- 谁确认通过
- 何时通过
- 退回原因
- 是否允许进入下一阶段

---

## 7. 后端业务模块设计

建议后端拆为 5 个服务域。

### 7.1 Project Service

负责：

- 项目创建
- 模板绑定
- 论文结构初始化
- 实验体系基础信息维护

### 7.2 Asset Service

负责：

- 文件上传
- 文件归档
- 文件版本管理
- 文件预览与解析结果挂载
- 文件与图表计划/章节/体系之间的关联

### 7.3 Workflow Service


负责：

- 状态机推进
- 门禁规则验证
- 审批任务生成
- 退回与重试
- 推进日志记录

这是系统的核心服务。

### 7.4 Evidence Service

负责：

- Figure/Table Plan
- Asset Manifest
- Claim–Evidence Matrix
- Outline 与 asset 的绑定关系

### 7.5 Draft Service

负责：

- 小节草稿生成记录
- 版本管理
- 用户批注
- 草稿审批状态
- 已确认文本冻结

---

## 8. 数据层设计

数据层应拆分为两类：

### 8.1 对象存储（Object Storage）

用于存储：

- PDF 文献
- 原始表格
- 图像文件
- 导出的图表
- Word/Markdown 草稿
- 分析结果附件

### 8.2 结构化数据库（Relational DB）

用于存储：

- 项目状态
- 实验体系卡
- 图表计划
- 证据矩阵
- 审批流
- 写作版本
- 任务日志

建议使用 PostgreSQL；如需高频任务调度，可配 Redis。

---

## 9. 核心数据表设计建议

下面给出建议的核心表结构。

### 9.1 项目层

#### `projects`

- id
- name
- owner_id
- status
- template_asset_id
- thesis_schema_json
- created_at
- updated_at

#### `project_members`

- id
- project_id
- user_id
- role

### 9.2 实验体系层

#### `experimental_systems`

- id
- project_id
- system_no
- title
- research_goal
- samples_subjects
- variables_controls
- output_metrics
- methods_summary
- system_card_json
- status

#### `system_sections`

- id
- system_id
- section_key
- title
- order_no

默认可生成：

- 引言
- 实验材料与方法
- 结果与讨论
- 本章小结

### 9.3 资产层

#### `assets`

- id
- project_id
- system_id
- asset_type
- file_name
- storage_key
- mime_type
- version
- uploaded_by
- created_at

#### `asset_metadata`

- id
- asset_id
- semantic_description
- source_description
- instrument_info
- sample_ids
- conditions_json
- qc_status

### 9.4 图表计划层

#### `figure_plans`

- id
- system_id
- figure_no
- title
- claim_text
- data_needed_json
- method_json
- acceptance_criteria_json
- status

#### `figure_plan_assets`

- id
- figure_plan_id
- asset_id
- role

### 9.5 证据矩阵层

#### `claims`

- id
- system_id
- claim_id
- statement
- section_ref
- confidence_level
- status

#### `claim_evidence_links`

- id
- claim_record_id
- asset_id
- statistical_support
- analysis_run_id

### 9.6 写作层

#### `outlines`

- id
- system_id
- version
- outline_json
- status

#### `outline_asset_bindings`

- id
- outline_id
- section_key
- asset_id

#### `section_drafts`

- id
- system_id
- section_key
- version
- content_md
- generated_from_claims_json
- status
- created_by_agent
- created_at

#### `review_comments`

- id
- draft_id
- commenter_id
- comment_text
- decision
- created_at

### 9.7 工作流层

#### `workflow_instances`

- id
- project_id
- system_id
- current_state
- started_at
- updated_at

#### `workflow_events`

- id
- instance_id
- event_type
- from_state
- to_state
- payload_json
- created_by
- created_at

#### `approval_tasks`

- id
- project_id
- system_id
- task_type
- target_id
- status
- reviewer_id
- decision_note
- created_at

---

## 10. 状态机设计

### 10.1 实验体系级状态机

每个实验体系独立推进，建议使用如下状态流：

```text
Draft
  ↓
System_Defined
  ↓
Figure_Plan_Ready
  ↓
Data_Pending
  ↓
Data_Uploaded
  ↓
Analysis_Ready
  ↓
Assets_Confirmed
  ↓
Evidence_Matrix_Ready
  ↓
Outline_Ready
  ↓
Section_Drafting
  ↓
Chapter_Review
  ↓
Chapter_Approved
```

### 10.2 各状态含义

#### `System_Defined`

实验体系卡已确认。

#### `Figure_Plan_Ready`

本体系的 Figure/Table Plan 已确认。

#### `Data_Pending`

系统已明确需要哪些数据，但用户尚未上传完整。

#### `Data_Uploaded`

本体系所需的核心原始数据、图像或表格已经到位。

#### `Analysis_Ready`

数据清洗、分析与统计报告已经生成。

#### `Assets_Confirmed`

图表、caption、semantic_description 均已确认。

#### `Evidence_Matrix_Ready`

关键结论已与证据完成绑定。

#### `Outline_Ready`

章节大纲及 asset 绑定已确认。

#### `Section_Drafting`

允许逐节写作与修改。

#### `Chapter_Review`

章节整体进入终审。

#### `Chapter_Approved`

本体系章节完成。

### 10.3 项目级状态机

项目整体建议采用更粗粒度状态流：

```text
Init
  ↓
Systems_Collecting
  ↓
Systems_Writing
  ↓
Intro_Conclusion_Ready
  ↓
Full_Thesis_Review
  ↓
Finalized
```

### 10.4 项目级关键规则

只有当**至少 3 个实验体系达到 `Chapter_Approved`** 后，系统才允许进入：

- 绪论写作
- 结论与展望写作
- 整篇论文总审

---

## 11. Agent 设计与编排

后端不建议使用一个“大一统 agent”，而应拆分为多个结构化 worker。

### 11.1 推荐的 worker 列表

1. `template-parser`
2. `literature-structure-extractor`
3. `system-card-builder`
4. `figure-plan-generator`
5. `data-auditor`
6. `vision-asset-checker`
7. `evidence-matrix-builder`
8. `micro-outline-writer`
9. `section-writer`
10. `consistency-checker`

### 11.2 这样拆分的好处

- 降低 prompt 复杂度
- 降低幻觉风险
- 更利于失败重试
- 更利于后端精确控制输入输出
- 更容易替换不同模型或不同执行器

### 11.3 写作 agent 的强约束

写作 agent 在任何时候都应遵守以下规则：

1. 只能基于已批准的 claim 写作
2. 每一段都必须能回指到 asset 或 analysis 结果
3. 不得写入 Evidence Matrix 中不存在的实验结论
4. 未通过 G4/G5 的章节不得生成结论性正文

---

## 12. Vision 与图表校验模块

这一部分应保留你原本的“AI 作图顾问 + 用户上传手工图 + 系统视觉校验”的设计思路。

### 12.1 Vision 模块建议承担的职责

- 检查图片是否有比例尺、坐标轴、图例、误差条等必要信息
- 检查上传图与图表 spec 是否一致
- 自动生成 `semantic_description`
- 自动生成 caption 草稿
- 提示缺失项（例如未标单位、未标对照、图片分辨率不足等）

### 12.2 不应让 Vision 模块承担的职责

Vision 模块不应直接得出“学术结论”，它只能做图像级描述和规范校验；结论必须由 Evidence Matrix 来统一管理。

---

## 13. API 设计建议

以下是推荐的核心接口。

### 13.1 项目与实验体系

- `POST /projects`
- `GET /projects/{id}`
- `POST /projects/{id}/systems`
- `PATCH /systems/{id}`

### 13.2 文件上传与绑定

- `POST /assets/upload`
- `GET /assets/{id}`
- `POST /assets/{id}/bind`

### 13.3 图表与证据

- `POST /systems/{id}/figure-plans/generate`
- `PATCH /figure-plans/{id}`
- `POST /systems/{id}/evidence-matrix/generate`
- `PATCH /claims/{id}`

### 13.4 写作与审批

- `POST /systems/{id}/outline/generate`
- `POST /systems/{id}/sections/{sectionKey}/draft`
- `POST /drafts/{id}/review`
- `POST /drafts/{id}/approve`

### 13.5 工作流

- `POST /systems/{id}/advance`
- `GET /systems/{id}/workflow`
- `GET /projects/{id}/dashboard`

---

## 14. 技术栈建议

### 14.1 MVP 版本

#### 前端

- Next.js
- React
- Tailwind CSS
- React Query

#### 后端

- FastAPI 或 NestJS
- PostgreSQL
- Redis
- 对象存储：S3 / MinIO

#### 异步任务与工作流

- Celery / BullMQ
- 如需更强流程控制，优先考虑 Temporal

#### Agent 执行层

- Claude Code
- Vision 模型
- Python 数据分析 worker

### 14.2 为什么推荐引入工作流引擎

因为你的场景不是简单 CRUD，而是：

- 长事务
- 可回退
- 有审批节点
- 有等待用户上传的暂停状态
- 有失败重试
- 有跨阶段依赖

这类流程非常适合 Temporal 一类工作流引擎，而不是零散接口手写串联。

---

## 15. MVP 范围建议

第一版不要做得过重，建议先实现最小闭环。

### 15.1 MVP 必做

1. 项目创建
2. 实验体系卡录入
3. 模板与参考文献上传
4. Figure/Table Plan 生成与确认
5. 数据上传与资产归档
6. Asset Manifest 生成
7. Claim–Evidence Matrix 生成与确认
8. 小节微大纲生成
9. 逐节草稿生成与审批
10. 状态机推进

### 15.2 MVP 暂不做

1. 全自动排版导出整篇终稿
2. 复杂文献知识库检索增强
3. 多人实时协作编辑
4. 全自动参考文献管理
5. 自动拼接所有 Supporting Information

---

## 16. 与原设计原则的关系

本次前后端改造不是推翻原设计，而是将原设计真正产品化。

### 16.1 被保留的部分

以下内容保持不变：

- Step 0–12 的总体流程思想
- G1–G5 门禁思想
- Figure/Table Plan 前置
- Raw Data Archive / Acquisition Log / QC Checklist
- Cleaned Tables / Preprocessing Log / Derived Metrics
- Analysis Report / Decision Notes
- Asset Manifest / Claim–Evidence Matrix
- Outline 绑定 asset id
- 逐节协作写作
- 写作必须严格基于证据

### 16.2 被强化的部分

本次改造新增并强化了以下内容：

1. **G0：实验体系门禁**
2. **实验体系是最小推进单元**
3. **前后端工作流化落地**
4. **Evidence Matrix 作为唯一事实源**
5. **审批、退回、重试、版本化机制**
6. **Claude Code 从“系统主体”降级为“后端执行器”**

### 16.3 被改变的部分

主要变化在于：

- 不再依赖单一对话项目容器保存全部状态
- 不再将模型上下文视为项目数据库
- 不再让写作直接面向所有上传内容自由发挥
- 改为：结构化工件先沉淀，再由 agent 受控写作

---

## 17. 最终推荐定义

综合来看，最适合你的系统定义是：

> **一个以实验体系为核心组织单元、以证据矩阵为唯一事实源、以前后端工作流平台为承载形式、以 Claude Code 为受控执行器的工科/实验型毕业论文写作系统。**

这一定义同时满足：

- 你的原始设计原则
- 工科实验型论文的章节组织逻辑
- 用户需要“无限确认直到满意”的真实使用流程
- 文献、数据、图表、草稿长期管理的产品化要求

---

## 18. 下一步实施建议

建议按以下顺序推进：

### 第一阶段：做 MVP

先跑通一个实验体系的完整闭环：

1. 建项目
2. 建体系卡
3. 生成图表计划
4. 上传数据
5. 生成证据矩阵
6. 生成大纲
7. 逐节写作
8. 审批通过

### 第二阶段：支持多个实验体系串行推进

完成：

- 多体系管理
- 至少 3 章独立推进
- 项目级状态汇总

### 第三阶段：支持绪论/结论的跨体系整合

在多个体系章节完成后：

- 自动汇总共性背景
- 汇总结论矩阵
- 生成结论与展望
- 做整篇一致性检查

### 第四阶段：增强文献与图像智能

后续可加入：

- 文献知识库/RAG
- 图像规范自动校验
- caption 自动优化
- 证据缺口提示
- 实验数据异常检测

---

## 19. 结论

你的“前端上传与监控 + 后端基于 Claude Code 执行”的方向总体是正确的，但最终更合理的方案不是“Claude Code 后端化”，而是：

> **前端工作台 + 业务后端/工作流引擎 + 文件与元数据存储 + Claude Code / Vision / 数据分析 Worker 的多执行器体系。**

这个方案能够真正承载你原有设计中最关键的要求：

- 以实验体系为章节模块
- 先图表与证据，后正文叙事
- 关键结论必须可追溯
- 写作必须受证据矩阵约束
- 每一步必须可确认、可退回、可重做、可版本化

这才是一个适合工科/实验型毕业论文的长期可用方案。


---

# 附录：MVP 技术栈职责说明

本节补充说明 MVP 方案中各技术栈在系统中的职责边界，避免将“前端框架”“工作流引擎”“Agent 执行器”“数据分析执行器”混用。整体上，这套系统应分为四层：前端交互层、业务后端层、异步编排层、智能执行层。

## 1. 前端

### 1.1 Next.js

**角色：前端应用框架**

Next.js 负责承载整个“论文工作台”前端应用，适合实现多页面、多路由、强交互的 Web 系统。  
在本系统中主要承担：

- 项目总览页、实验体系管理页、文件上传页、图表与证据面板、章节写作与审批页等页面组织
- 路由管理，如项目页、实验体系页、草稿页等
- 作为前端与后端 API 的交互入口

可以理解为：**Next.js 是整个产品前端的骨架。**

### 1.2 Tailwind

**角色：前端样式系统**

Tailwind 负责界面样式表达，而不负责业务逻辑。  
在本系统中主要用于：

- 卡片、表单、表格、按钮、状态标签等组件的样式组织
- 门禁状态（待上传、待分析、已确认、退回修改等）的可视化
- 页面布局和响应式适配

可以理解为：**Tailwind 负责把前端页面快速做出来，并保持视觉一致性。**

### 1.3 React Query

**角色：前端数据请求与状态同步层**

React Query 负责管理前端如何请求后端接口、缓存数据、自动刷新和同步状态。  
在本系统中主要用于：

- 获取项目详情、实验体系列表、草稿详情、工作流状态
- 提交“生成 Figure Plan”“生成草稿”“审批通过”等操作后的状态刷新
- 管理 loading / success / error 状态
- 配合轮询异步任务结果

可以理解为：**React Query 负责前端与后端数据交互的秩序。**

---

## 2. 后端

### 2.1 FastAPI 或 NestJS

**角色：后端主框架 / API 与业务入口**

这是整个系统的业务后端骨架。它负责把项目、实验体系、文件、图表、证据矩阵、草稿、审批、任务等业务能力组织成统一 API。

主要承担：

- 用户请求入口
- 项目、实验体系、资产、草稿、审批等 API
- 访问 PostgreSQL、Redis、对象存储
- 调度异步任务
- 调用 Claude Code、图像理解模型、Python 分析 worker

#### FastAPI 适合的情况

- 后端主要以 Python 为主
- 数据分析、文件处理、模型调用较多
- 希望和 Python 数据分析 worker 紧密协同

#### NestJS 适合的情况

- 团队更偏 TypeScript / Node.js
- 更强调模块化、依赖注入、工程化结构
- 希望前后端语言统一

结论上：  

- **偏 AI/分析驱动：优先 FastAPI**  
- **偏工程化 Web 产品：优先 NestJS**

### 2.2 PostgreSQL

**角色：核心结构化数据库**

PostgreSQL 用来存放系统的业务真相，而不是存大文件本体。

主要存储：

- 项目
- 实验体系卡
- Figure Plan
- Claim–Evidence Matrix
- 草稿版本
- 审批记录
- 工作流状态
- 用户信息
- 文件元数据

由于本系统是一个“强状态、强门禁、强证据追溯”的工作流系统，因此 PostgreSQL 是核心基础设施。

可以理解为：**PostgreSQL 存系统的业务真相。**

### 2.3 Redis

**角色：缓存 + 临时状态 + 任务协作层**

Redis 不是主数据库，而是一个高性能中间层。

主要承担：

- 缓存热点数据（项目概览、当前体系状态等）
- 作为任务队列中间件的一部分
- 保存短期状态（上传进度、任务执行中间状态、轮询结果）
- 实现分布式锁、限流、防重复提交

可以理解为：**Redis 负责高频、短期、快速访问的数据协调。**

### 2.4 S3 / MinIO

**角色：对象存储 / 文件仓库**

对象存储负责保存文件本体，而不是结构化业务数据。

适合存放：

- PDF 文献
- 论文模板
- 原始 Excel / CSV
- 手工图、图像类表征结果
- 自动生成的分析结果文件
- 章节导出文件

#### S3

适合云上生产环境。

#### MinIO

兼容 S3 协议，适合本地部署、测试环境或内网环境。

可以理解为：**S3 / MinIO 是论文相关文件的仓库。**

---

## 3. 异步任务与工作流

系统中很多操作不适合同步阻塞执行，例如：

- PDF 解析
- Figure Plan 生成
- 图片校验
- 数据清洗与统计分析
- Evidence Matrix 构建
- 章节草稿生成
- 一致性检查

这些能力应放入异步任务或长流程编排系统。

### 3.1 Celery

**角色：Python 任务队列**

适用于：

- 后端采用 FastAPI / Python
- 需要大量运行数据分析、文件处理、模型调用等任务

典型任务：

- `parse_template`
- `run_analysis`
- `build_evidence_matrix`
- `generate_section_draft`

可以理解为：**Celery 适合 Python 后端的异步任务执行。**

### 3.2 BullMQ

**角色：Node.js 任务队列**

适用于：

- 后端采用 NestJS / Node.js
- 任务调度以 JS/TS 生态为主

典型任务：

- 文件处理
- Agent 调度
- 任务重试
- 工作流推进中的短任务执行

可以理解为：**BullMQ 是 Node.js 生态中的任务队列方案。**

### 3.3 Temporal

**角色：长流程编排引擎**

Temporal 与 Celery/BullMQ 不完全处于同一层级。Celery/BullMQ 更偏任务执行，而 Temporal 更适合管理“持续时间长、包含审批与等待用户输入、可能反复退回”的业务流程。

你的论文系统正好具备这些特征：

- 体系定义
- 图表计划
- 等待用户上传数据
- 分析
- 资产确认
- 证据矩阵
- 大纲
- 草稿
- 审批
- 退回修改
- 再次流转

因此 Temporal 非常适合做“论文工作流引擎”。

可以理解为：**Temporal 不是单纯跑任务，而是管理整条论文生产流程。**

---

## 4. Agent 执行层

### 4.1 Claude Code

**角色：通用智能执行器 / 编排型 Agent**

Claude Code 适合承担“需要理解文档、组织信息、生成结构化内容”的任务，但不应独自承担项目状态管理、权限、审批、数据库真相等系统职责。

适合做：

- 解析模板结构
- 提取参考论文章节骨架
- 根据用户自然语言生成实验体系卡
- 生成 Figure Plan
- 生成章节微大纲
- 基于 Evidence Matrix 写作章节草稿
- 做一致性和结构审查

可以理解为：**Claude Code 是智能执行器，而不是整个系统本身。**

### 4.2 图像理解模型

**角色：视觉证据解析器**

本系统中的很多证据以图片形式存在，例如：

- 显微图
- 谱图
- 曲线截图
- 装置图
- 手工拼图
- 表征图像

图像理解模型适合承担：

- 检查图中是否具备比例尺、坐标轴、图例等基本要素
- 判断图像是否符合 Figure Spec
- 生成 `semantic_description`
- 生成 caption 初稿
- 提取图像结构信息供写作和校验使用

不建议让图像理解模型直接输出高层学术结论。  
它更适合作为图像资产的结构化解析器。

可以理解为：**图像理解模型负责把图片证据转化为系统可用的结构化描述。**

### 4.3 Python 数据分析 worker

**角色：实验数据处理与统计执行器**

Python 数据分析 worker 是实验型论文系统中非常关键的一层，负责把原始实验数据变成可引用的分析结果。

主要承担：

- 读取 CSV / Excel
- 数据清洗
- 缺失值/异常值处理
- 计算衍生指标
- 统计检验
- 生成结构化分析结果
- 根据 spec 自动绘制部分标准图表
- 输出 Analysis Report

之所以单独设置这一层，是因为实验型论文的可信性来自**可复现的数据分析过程**，而不仅是语言模型生成的文字。

可以理解为：**Python worker 负责把原始实验数据转化为可引用的分析结论与图表结果。**

---

## 5. 各层协作关系

这套 MVP 的典型协作流程如下：

1. 用户在前端创建项目、填写实验体系、上传模板/文献/数据/图片  
2. 后端接收请求，将结构化信息写入 PostgreSQL，将文件写入 S3 / MinIO  
3. 后端把分析、校验、写作等任务提交到 Celery / BullMQ / Temporal  
4. 执行层分别由 Claude Code、图像理解模型、Python worker 处理对应任务  
5. 任务结果回写数据库与对象存储，形成 Figure Plan、Asset Manifest、Evidence Matrix、Draft 等工件  
6. 前端通过 React Query 获取最新结果，供用户审批、退回和继续推进

---

## 6. 推荐选型建议

### 6.1 偏 AI + 数据分析优先

推荐：

- 前端：Next.js + Tailwind + React Query
- 后端：FastAPI
- 数据库：PostgreSQL
- 缓存：Redis
- 对象存储：MinIO（开发）/ S3（生产）
- 异步任务：Celery
- 长流程升级：Temporal
- 执行层：Claude Code + 图像理解模型 + Python 数据分析 worker

### 6.2 偏工程化 Web 产品优先

推荐：

- 前端：Next.js + Tailwind + React Query
- 后端：NestJS
- 数据库：PostgreSQL
- 缓存：Redis
- 对象存储：MinIO / S3
- 异步任务：BullMQ
- 长流程升级：Temporal
- 执行层：Claude Code + 图像理解模型 + Python 数据分析 worker

---

## 7. 本系统成败最关键的三层

相较于单个技术栈的选择，本系统更关键的是以下三层是否清晰分离：

1. **业务状态层**：PostgreSQL + 工作流状态机  
2. **文件证据层**：S3 / MinIO + Asset Metadata  
3. **智能执行层**：Claude Code + 图像理解模型 + Python worker  

只有这三层边界清晰，才能真正落地“实验体系驱动、证据矩阵约束、门禁推进”的论文写作系统。

